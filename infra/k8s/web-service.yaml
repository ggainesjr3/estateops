apiVersion: v1
kind: Service
metadata:
  name: estateops-web
  namespace: estateops
  labels:
    app.kubernetes.io/name: estateops
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
